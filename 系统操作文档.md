# 智能制造 ERP 系统操作文档

## 1. 文档目标

本文档用于演示版环境的日常启动、登录、页面操作和角色试用说明，适合客户演示、内部试用和培训使用。

## 2. 系统访问说明

### 2.1 本地开发访问

- 前端地址：`http://localhost:5173/` 或当前 Vite 实际启动端口
- 后端接口前缀：`/api`
- 登录页：`/login`

### 2.2 公网演示访问

如已配置 frp/内网穿透，请使用演示域名访问前端页面；前端会通过 `/api` 代理访问后端。

## 3. 启动与初始化

### 3.1 首次安装

1. 安装依赖
   - `npm install`
2. 启动基础依赖
   - `docker compose up -d`
3. 初始化数据库
   - `npm --workspace @smart-erp/api run prisma:generate`
   - `npm --workspace @smart-erp/api run prisma:migrate`
   - `npm --workspace @smart-erp/api run prisma:seed`

### 3.2 启动应用

- 启动后端：`npm --workspace @smart-erp/api run dev`
- 启动前端：`npm --workspace @smart-erp/web run dev`

### 3.3 演示数据重置

如需恢复演示环境到初始状态，重新执行：

- `npm --workspace @smart-erp/api run prisma:seed`

说明：

- 当前演示数据采用“覆盖重置”策略。
- 重新 seed 后，系统会重建演示角色、演示账号、工单、采购、质量、设备、报表等数据。
- 前端已兼容 reseed 后工厂 ID 变化的情况，重新登录即可继续使用。

## 4. 演示账号

### 4.1 管理员账号

- 用户名：`admin`
- 密码：`Admin@123`
- 适用场景：完整演示、配置检查、用户与角色管理

### 4.2 业务演示账号

统一密码：`Demo@123`

| 用户名        | 显示名   | 角色       | 适用演示场景                      |
| ------------- | -------- | ---------- | --------------------------------- |
| `planner`   | 周晓岚   | 计划经理   | 主数据、工单、排程、MRP、库存联动 |
| `warehouse` | 陈立仓   | 仓储主管   | 库存、采购、报表                  |
| `quality`   | 赵敏质检 | 质量经理   | 质量检验、报表、AI 助手           |
| `equipment` | 孙维保   | 设备经理   | 设备台账、维保、报表              |
| `finance`   | 林成本   | 成本分析员 | 成本、报表                        |

## 5. 角色与菜单权限

系统已启用“菜单 + 页面”双层控制：

- 左侧菜单只显示当前角色可访问页面
- 即使手工输入 URL，未授权页面也会自动跳回可访问首页

### 5.1 ERP 管理员

可见菜单：

- 运营驾驶舱
- 生产看板
- 主数据中心
- BOM 管理
- 工艺路线
- MRP 运算
- 工单与报工
- 排程管理
- 库存与采购
- 质量中心
- 设备中心
- 成本管理
- 集成监控
- 报表与导出
- AI 助手
- 用户管理

### 5.2 计划经理

可见菜单：

- 运营驾驶舱
- 主数据中心
- 生产看板
- MRP 运算
- 工单与报工
- 排程管理
- 库存与采购
- 报表与导出
- AI 助手

### 5.3 仓储主管

可见菜单：

- 运营驾驶舱
- 库存与采购
- 报表与导出

### 5.4 质量经理

可见菜单：

- 运营驾驶舱
- 质量中心
- 报表与导出
- AI 助手

### 5.5 设备经理

可见菜单：

- 运营驾驶舱
- 设备中心
- 报表与导出

### 5.6 成本分析员

可见菜单：

- 运营驾驶舱
- 成本管理
- 报表与导出

## 6. 主要业务页面操作说明

### 6.1 运营驾驶舱

用途：查看当前工厂的关键经营指标。

关注内容：

- 交付达成率
- 设备 OEE
- 风险工单
- 待检数量
- 工单状态分布
- 设备关注列表

建议演示方式：

- 先用 `admin` 或 `planner` 登录，展示系统总览
- 再切换为 `warehouse`、`finance` 等账号，展示不同角色首页一致但菜单权限不同

### 6.2 主数据中心

用途：查看和维护物料、供应商、仓库等基础资料。

适合角色：

- `admin`
- `planner`

### 6.3 BOM 管理 / 工艺路线

用途：查看产品结构与工艺流程。

适合演示内容：

- 成品与原料/半成品关系
- 不同工序、工作中心、标准工时

适合角色：

- `admin`
- `planner`

### 6.4 生产看板 / 工单与报工 / 排程管理

用途：展示多状态工单、排产情况与报工进度。

当前演示数据覆盖：

- `DRAFT`
- `RELEASED`
- `IN_PROGRESS`
- `PAUSED`
- `COMPLETED`
- `CLOSED`

适合角色：

- `admin`
- `planner`

### 6.5 MRP 运算

用途：根据库存余额与安全库存生成采购建议。

操作步骤：

1. 打开“MRP 运算”页面
2. 点击“重新运行”
3. 查看建议采购清单

适合角色：

- `admin`
- `planner`

### 6.6 库存与采购

用途：查看采购单、库存交易和在库情况。

当前演示数据覆盖：

- 采购状态：`DRAFT`、`APPROVED`、`SENT`、`PARTIAL_RECEIVED`、`RECEIVED`
- 库存事务：`RECEIPT`、`ISSUE`、`TRANSFER`、`ADJUSTMENT`、`RETURN`、`SUPPLEMENT`

适合角色：

- `admin`
- `planner`
- `warehouse`

### 6.7 质量中心

用途：查看来料、过程、终检数据。

当前演示数据覆盖：

- 检验类型：`IQC`、`IPQC`、`FQC`
- 检验状态：`PENDING`、`PASS`、`FAIL`

适合角色：

- `admin`
- `quality`

### 6.8 设备中心

用途：查看设备状态与维保工单。

当前演示数据覆盖：

- 设备状态：`RUNNING`、`IDLE`、`STOPPED`、`MAINTENANCE`
- 多张维保工单

适合角色：

- `admin`
- `equipment`

### 6.9 成本管理

用途：查看工单成本与采购成本汇总。

适合角色：

- `admin`
- `finance`

### 6.10 报表与导出

用途：查看生产、质量、库存、采购图表并导出 Excel。

适合角色：

- `admin`
- `planner`
- `warehouse`
- `quality`
- `equipment`
- `finance`

### 6.11 AI 助手

用途：演示 AI 经营摘要、排产建议与异常分析能力。

适合角色：

- `admin`
- `planner`
- `quality`

### 6.12 用户管理

用途：维护用户、停用用户、分配角色。

适合角色：

- `admin`

## 7. 推荐演示路径

### 7.1 全量演示路径（管理员）

建议顺序：

1. 运营驾驶舱
2. 主数据中心
3. BOM 管理
4. 工艺路线
5. 工单与报工
6. 排程管理
7. MRP 运算
8. 库存与采购
9. 质量中心
10. 设备中心
11. 成本管理
12. 报表与导出
13. AI 助手
14. 用户管理

### 7.2 角色权限演示路径

建议顺序：

1. 用 `admin` 登录，展示全量菜单
2. 退出后用 `warehouse` 登录，展示仅保留库存/报表菜单
3. 手动输入 `/users` 或 `/equipment`，展示会被拦回首页
4. 再用 `finance` 登录，展示仅保留成本/报表菜单

## 8. 常见问题处理

### 8.1 登录后页面没有数据

处理方式：

1. 刷新页面
2. 重新登录
3. 如刚执行过 seed，优先重新登录一次

### 8.2 reseed 后演示账号是否会丢失

不会。重新执行 seed 后会自动重建以下账号：

- `admin`
- `planner`
- `warehouse`
- `quality`
- `equipment`
- `finance`

### 8.3 为什么不同账号看不到相同菜单

这是系统当前的权限设计，不是异常。
系统会根据登录角色自动裁剪菜单与页面访问权限。

### 8.4 为什么直接访问某个页面会跳回首页

说明当前角色没有该页面权限，属于预期行为。

## 9. 维护建议

- 演示前先执行一次 `npm --workspace @smart-erp/api run prisma:seed`
- 演示前确认前后端服务均已启动
- 演示时优先使用上海一厂数据
- 如要展示完整能力，优先使用 `admin`
- 如要展示权限隔离，优先使用 `warehouse` 与 `finance`
